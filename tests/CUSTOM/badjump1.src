# Sample program with unreachable jumps
.IPPcode23

# Infinite loop
# DEFVAR GF@result # undefined variable, parser should still pass
LABEL loop
ADD GF@result int@1 int@1
JUMP loop # 1 backjump

# These jumps are not reachable
JUMP non_existant_label_1
JUMP non_existant_label_2